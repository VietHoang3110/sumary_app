import 'package:flutter/material.dart';

import '../../../../src/src_index.dart';

class widget_adviser extends StatelessWidget {
  const widget_adviser({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}